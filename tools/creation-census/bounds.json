{
  "$comment": "Pinned bounds for the creation census. EVERY value carries a stated reason in `justification`, keyed `<group>.<key>`, and test/creation-census.test.ts fails if one is missing or a stub. 'No measurement backs this, and here is what would' is an acceptable reason; inventing an anchor is not.",
  "version": 1,
  "pinnedAt": "2026-08-04",
  "census": {
    "queryId": 8214953,
    "savedQueryName": "slot-zero: creation census - deployers creating in one month (COHORT_SQL)",
    "defaultMonth": "2026-07",
    "minLaunches": 30,
    "maxRows": 5000,
    "maxRowsCeiling": 20000,
    "resultLimitHeadroom": 64
  },
  "dune": {
    "maxExecutionsPerRun": 1,
    "maxRequestsPerRun": 48,
    "pollIntervalMs": 3000,
    "maxPollAttempts": 40,
    "minIntervalMs": 250,
    "privateQuerySlots": 10,
    "worstCaseCreditsPerExecution": 25,
    "resultBytesPerRowCeiling": 121,
    "allowanceReserveCredits": 25,
    "monthlyCreditCapCredits": 4000,
    "allowanceTightMultiple": 2,
    "allowanceRequired": true
  },
  "justification": {
    "dune.monthlyCreditCapCredits": "THE CAPTAIN'S OWN MONTHLY CEILING, IN CONFIGURATION BECAUSE THAT IS THE ONLY PLACE IT MAY LIVE. Captain decision 322a, 2026-08-06: 4,000 credits a month, not to be exceeded and not to be left unspent either. NOT A MEASUREMENT: it is the operator's stated budget, so this entry names its source rather than an anchor. THE SAME NUMBER UNDER THE SAME KEY IS PINNED IN tools/deployer-screen/thresholds.json -> dune.monthlyCreditCapCredits, and test/dune-credit-ceiling.test.ts pins the two equal — neither keyed tool may import the other, so one number across both is a duplicated value held together by a test, exactly as the credit guard's own text is. An operator changing the cap changes BOTH. HOW IT BINDS: client.mjs -> bindingCreditCeiling takes the SMALLER of this and the vendor's reported credits_included for the current billing period, and decideAllowance measures that period's own credits_used against it — which enforces a monthly total on a tool that holds no state between runs, since the vendor's counter is the ACCOUNT's running total rather than a run's. Both figures reach every verdict, so a refusal states which ceiling bound and whether raising the cap or waiting for the period to roll is the fix. WHAT IT ENFORCES IS PER ACCOUNT-PERIOD RATHER THAN FLEET-WIDE: the fleet holds more than one Dune key, separate keys are separate accounts with their own quotas and their own period boundaries, and each counter sees only its own spend, so two keys each honouring this cap spend twice it between them. Closing that needs one key or a smaller cap on each, and both are the captain's decision. NO VENDOR FIGURE OR PERIOD IS WRITTEN DOWN HERE: the guard reads both live from whichever key this lane's credential resolves to. This lane DEFERS rather than falling back when a ceiling refuses it — it has no keyless substitute for the enumeration.",
    "census.queryId": "The saved Dune query holding COHORT_SQL, deployed 2026-08-04 by `run.mjs --deploy --live` under captain decision 187a. `null` before deployment rather than a guessed id, so nothing can execute the wrong statement under this name. IT WAS RECORDED AS UNDEPLOYABLE AND THAT WAS FALSE: `tools/arrival-rate-walk/cohort.mjs`, that lane's README and CLAUDE.md all said the Free tier's ten private query slots were full and the account held ten. The account held EIGHT, six of them retired scratch probes, and the stale sentence is why this widening sat undone for a month. Do not replace it with another number to be taken on faith: the count is re-checkable at any time with `GET https://api.dune.com/api/v1/queries?limit=100` (header `X-Dune-API-Key`), whose `total` field is the figure, and `run.mjs --deploy` counts it itself immediately before creating anything and refuses rather than asserting.",
    "census.savedQueryName": "The name the deploy step gives the saved query. It says which repository constant the text came from, because the account is shared with scratch probes and a query whose provenance is not in its own name is one a later reader has to diff to identify. It is not load-bearing: the text is what is verified, byte for byte, before every execution.",
    "census.defaultMonth": "2026-07, the month captain decision 187a's own case is measured on: the current feed sees 25 deployers creating in it against the census's 10,280, and at the Stage 1 competence gate 8 against 35 - the 27 named wallets this project structurally could not see (`data/slot-zero-discovery-widen-operations/report.md` sections 2.1 and 4.1). It is also the month whose deployers are screenable TODAY, which is what the captain's question asks about ('can I beat the dev ... currently'). NOTE THE OTHER LANE'S MONTH IS DIFFERENT AND DELIBERATELY SO: `tools/arrival-rate-walk/bounds.json` seeds from 2026-01 because it needs forward OBSERVATION time, not current relevance, and widening there goes backwards. One census month is one execution, so serving both is a second run rather than a compromise.",
    "census.minLaunches": "30 creations in the seed month. IT IS A PROLIFIC-NESS CUT, NOT A COMPETENCE ONE - `census.mjs` -> PROLIFIC_CUT_CAVEAT is the sentence and it travels to the record, the rendered summary and the README. It selects on volume in one past month and on nothing else: no bonding rate, no completion, no 'still active' term, so it carries no survivorship bias, unlike the vendor feed's filter which looked like quality and was tempo. What it DOES miss is the low-cadence deployer, invisibly. The VALUE is chosen for result size rather than for meaning: the measured ladder for 2026-07 is 10,280 deployers at >=8, 5,416 at >=15 and 3,036 at >=30, and 3,036 rows at the ~100 bytes/row this repo has measured is ~0.3 MB, about 6 export credits against a 2,500-credit monthly free tier. A floor of 8 would be ~17 credits for the same answer plus a longer tail nothing downstream can walk. Tuning the sample SIZE is legitimate and is disclosed here; tuning it to reach a finding is not, and nothing in this lane reads the result before choosing.",
    "census.maxRows": "5,000 deployer rows per result, and the unit that matters is BYTES: Dune bills results at ~20 credits/MB and retrieving them is ~71% of the bill. At the measured 3,036 qualifying deployers for 2026-07 at the pinned floor, 5,000 is ~65% headroom - enough that a month running heavier than the probe does not truncate, and low enough that the read stays a single-digit-credit one. A result that DOES hit the cap is refused rather than published: the SQL carries the qualifying count before the cap as its own row, so truncation is detected exactly, and the surviving rows would be the most prolific ones, which is a selection rather than a shortfall.",
    "census.maxRowsCeiling": "20,000, the ceiling `--max-rows` may not be raised past from the command line. It is roughly the whole free tier's monthly export budget in one read (20,000 rows at ~100 bytes/row is ~2 MB, ~40 credits), so a census larger than this is a decision about the month's allowance rather than a flag on one run. A run now PRICES itself against the period's remaining credits before it spends (`dune.allowanceRequired` and README.md -> 'The monthly credit ceiling'), but NOTHING HERE TRACKS THE PERIOD ACROSS RUNS - the tool is stateless between them - which is exactly why the per-run ceiling has to be a real one.",
    "census.resultLimitHeadroom": "64 rows added to `max_rows` when the result is read. The `?limit=` must sit STRICTLY above the largest result the SQL can produce or a complete result reads as one truncated at its own limit and is refused - the read's own wholeness check treats rows-equal-to-limit as unprovable. The SQL returns at most `max_rows` deployer rows plus its coverage and total rows, so 64 covers today's three meta rows with room for a probe row a later version adds. It costs nothing: the limit bounds the read, the SQL bounds the result.",
    "dune.maxExecutionsPerRun": "1. AN EXECUTION IS BILLED WHETHER OR NOT IT SUCCEEDS AND IS NEVER RETRIED, and one census month is one execution by construction - the coverage probe rides in the same result as the count rather than costing a second one. A run that could spend two executions could spend two bills for one answer, so the ceiling is the design rather than a safety margin. Captain decision 187a authorised one census month at low single-digit credits; this is that bound in code.",
    "dune.maxRequestsPerRun": "48 requests of every kind, and the number is DERIVED rather than chosen: the ceiling must COVER a whole successful run, which is 1 credit-guard POST /usage + 1 saved-query verification + 1 execution POST + up to `dune.maxPollAttempts` status polls + 1 result read, plus headroom for `getJson`'s single 5xx/transport retry. That is `maxPollAttempts + 5` and this value must never sit below it - RAISING `maxPollAttempts` WITHOUT RAISING THIS IS HALF A CHANGE. The failure it exists to prevent is the worst shape a metered surface has: at 40 the request ceiling bound first, so an execution completing on a late poll exhausted the budget BEFORE the result read and the run burned a billed, unrecoverable execution and then refused to read its own answer - the money is spent and the answer is thrown away. Requests are not the billed unit here (bytes and executions are), so above that floor this bounds only wall clock, a wedged poll loop and polite use of a shared host.",
    "dune.pollIntervalMs": "3,000 ms between status polls. NO MEASUREMENT BACKS THIS NUMBER, and here is what would: the distribution of this query's own execution time over several months, which does not exist until it has run several times. It is chosen against the screen's experience that a scan of these tables completes in tens of seconds, and a poll is a read - it costs no credits - so the interval trades a little latency for a shorter poll loop.",
    "dune.maxPollAttempts": "40 polls, so ~2 minutes of waiting at the pinned interval before the run gives up. The execution is billed by then and is not retried; a later run can still read the saved query's cached result, which is why giving up here is cheap. It is deliberately larger than any execution this repository has measured, because timing out early on a slow-but-fine execution wastes the bill entirely.",
    "dune.minIntervalMs": "250 ms between request starts, the same pacing the screen's Dune client uses and for the same reason: this is a shared host and a free tier, and nothing in this lane is latency-sensitive. No shed event has been observed at it across either lane's runs.",
    "dune.worstCaseCreditsPerExecution": "25 credits, the WORST case this census's single execution may cost, and the input the monthly-ceiling guard converts a plan into money with (client.mjs -> estimatePlanCredits). It is pinned per lane because DUNE PUBLISHES NO PRICE TABLE FOR EXECUTION COMPUTE and the cost depends entirely on what the statement scans. MEASURED for the neighbouring lane's queries over the same decoded create surfaces, 2026-08-03: enumeration 0.919 execution credits, coverage probe 0.751; this census's own committed run (runs/2026-07-census.json) read 188,232 bytes for 3,036 rows in one execution. The widest execution any slot-zero lane has recorded that does NOT join the trade tape is 19.25 credits, and the two that DID join `dex_solana.trades` cost 81.74 and 221.51 (`slot-zero-launch-venue-daily-rates` section 7, held in firstmate's records, not in this repo). 25 is therefore just above the widest non-trade-join execution on record and roughly 27x this lane's own measured shape. COHORT_SQL MAY NOT GROW A TRADE JOIN WITHOUT MOVING THIS NUMBER.",
    "dune.resultBytesPerRowCeiling": "121 bytes per result row, the bound the credit guard multiplies the read limit by to price the EXPORT half of a run's worst case. MEASURED on this lane's own committed run: 188,232 bytes over 3,036 deployer rows is ~62 bytes/row, so 121 is ~2x the shape actually returned, and it is deliberately the same figure the screen pins for its wider six-column row rather than a tighter number of this lane's own — a census row is `deployer` plus a count, and one ceiling shared across both keyed lanes is one number to move when Dune changes how it serialises a result. It is a CEILING, not an estimate: the read is issued at `?limit=` census.maxRows + census.resultLimitHeadroom, so worst-case bytes are that limit x this x the reads a run may issue.",
    "dune.allowanceReserveCredits": "25 credits held back from every allowance reading before the plan is compared against it, and it exists because THE COUNTER LAGS. Measured on this account during the Dune evaluation: `credits_used` rose +6.0 while the evaluator was completely idle, and it lands in whole-credit jumps — so a reading understates spend, therefore OVERSTATES what remains, and a guard that spent to the last credit would be spending money the vendor has already taken. It also absorbs part of a gap it cannot measure: the key is SHARED, so another holder may spend between our reading and our execution. NO MEASUREMENT FIXES THE MULTIPLE and it is stated rather than dressed up: 25 is ~4x the single observed drift and 1% of the free tier's 2,500. What would justify a value: readings taken immediately before and after executions of known cost, which would price the lag distribution instead of one sample of it. The failure direction is the safe one — too large a reserve refuses a census that would have fitted, and a census refused before its execution costs nothing and can be re-run.",
    "dune.allowanceTightMultiple": "2. NOT A REFUSAL BAR — the threshold at which a run that WILL proceed prints a warning. A run whose worst case fits once but not twice may be the last one the period can afford, and an operator queueing heavier work behind it should learn that before the allowance is gone rather than from an HTTP 402 mid-run. NO MEASUREMENT BACKS THE 2, and here is what would: the distribution of Dune-spending runs per billing period, which does not exist because nothing here has yet run on a schedule. 2 is the smallest multiple that expresses 'this run is repeatable', which is the property that makes a failure recoverable.",
    "dune.allowanceRequired": "`true` — the census REFUSES when POST /usage cannot be read, rather than executing blind. NOT A MEASUREMENT, a policy. An unreadable balance is not headroom, and 'we could not see it' is the same evidence as 'it is empty'. THIS LANE HAS NO FALLBACK — unlike the screen, which walks the Solana RPC when Dune refuses, a census with no Dune answer is no census — so the cost of refusing is one deferred run, while the cost of proceeding blind is a billed execution that returns nothing and cannot be retried this period. The reading itself is free (Dune documents /usage as a metadata endpoint consuming no credits) and is retried once, so a single transport hiccup does not reach this switch.",
    "dune.privateQuerySlots": "10, the Free tier's private-query allowance, which the deploy step counts the account against BEFORE creating anything. This number is the vendor's and can change; what makes the check honest is that the CURRENT usage is read live from `GET /api/v1/queries?limit=100` rather than asserted here. If the two disagree the deploy refuses and prints both, and it never deletes or archives an existing query to make room - which of them is retired is not this tool's call."
  }
}
